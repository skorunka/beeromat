<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan
at `specs/013-matches-doubles-prematch/plan.md` (the v1.13 spec —
extends 012's matches feature with doubles support + a pre-match
agreement flow with explicit for-beer toggle; reuses the 012
matches/bet-transfer schema and pipeline). Companion artifacts in the
same directory: `spec.md`, `research.md`, `data-model.md`,
`quickstart.md`, `contracts/match-agreements.md`,
`checklists/requirements.md`. Earlier shipped features live at
`specs/001-beer-consumption-ledger/` through
`specs/012-matches-banter/` — their `plan.md` / `data-model.md` /
`contracts/` remain the source of truth for the data model, server
action contracts, and prior decisions that v1.13 layers on top of.
Constitution at `.specify/memory/constitution.md` (v1.7.0 — Fresh Code
Hygiene principle).
<!-- SPECKIT END -->
