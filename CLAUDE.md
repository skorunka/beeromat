<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current spec
at `specs/009-fresh-install-onboarding/spec.md` (the v1.9 spec —
fresh-install onboarding wizard at `/setup` that closes spec 008's
deploy-time-club-row precondition). Plan + tasks + companion artifacts
will land in the same directory as `/speckit-plan` and `/speckit-tasks`
run. Earlier shipped features live at
`specs/001-beer-consumption-ledger/` through `specs/008-admin-config/` —
their `plan.md` / `data-model.md` / `contracts/` remain the source of
truth for the data model, server action contracts, and prior decisions
that v1.9 layers on top of. Constitution at
`.specify/memory/constitution.md` (v1.7.0 — Fresh Code Hygiene principle).
<!-- SPECKIT END -->
